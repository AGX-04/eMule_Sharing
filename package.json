{
  "name": "emule-sharing-site",
  "type": "module",
  "version": "1.0.0",
  "description": "ED2K纪录片索引网站",
  "scripts": {
    "dev": "vitepress dev",
    "build": "node generateSidebar.cjs && vitepress build && pagefind --source .vitepress/dist" 
  },
  "dependencies": {
    "vitepress": "^1.0.0",
    "markdown-it-task-checkbox": "^1.0.6",
    "vitepress-plugin-pagefind": "^0.4.14" 
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}